/**
 * Generates placeholder config files
 * @returns false if generated placeholder needs to be filled before bot can work
 */
export function generateConfigPlaceholders(): Promise<boolean> {
	return new Promise((resolve) => {
		resolve(true)
	})
}
